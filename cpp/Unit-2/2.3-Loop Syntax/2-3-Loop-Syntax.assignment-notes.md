## Assignment Notes

### 2.3 Loop Syntax — Assignment Overview

#### Context

In this assignment, you'll practice using loops to solve a math problem. You'll write a program that sums all the multiples of a given number less than 100. This will help you understand loop syntax and logic in programming.

---

### Original Assignment Requirements (C++)

**Scenario:**
Sue's brother Steve has math homework to add all the multiples of a given number less than 100. Sue writes a program to validate his answers.

**Example:**
```
What multiples are we adding? 5
The sum of multiples of 5 less than 100 are: 950

What multiples are we adding? 7
The sum of multiples of 7 less than 100 are: 735
```

**Assignment:**
- Prompt the user for a number (the multiple).
- Use a loop to sum all multiples of that number less than 100.
- Output the result in the specified format.
- Run the test bed: `testBed cs124/assign23 assignment23.cpp`
- Submit your assignment with the name “Assignment 23” in the header.

**Hint:**
See page 162 for a hint (likely about loop structure or summing multiples).

**Requirements:**
- Use a loop to solve the problem.
- Follow University coding standards (see Appendix A).
- Include your name and assignment details in the header.
- Ensure your code runs without errors.

---

### JavaScript Equivalent Assignment

**Scenario:**
Write a JavaScript program that prompts the user for a number and sums all multiples of that number less than 100. Output the result in the specified format.

**Example:**
```
What multiples are we adding? 5
The sum of multiples of 5 less than 100 are: 950

What multiples are we adding? 7
The sum of multiples of 7 less than 100 are: 735
```

**Assignment:**
- Prompt the user for a number (using `readline-sync` or similar for Node.js).
- Use a loop to sum all multiples of that number less than 100.
- Output the result in the specified format.
- Follow JavaScript coding standards as specified by your instructor.
- Include your name and assignment details as comments at the top of the file.
- Ensure your code runs without errors.

**Process:**
1. Download the zip folder with all the contents included, then open the folder in VS Code.
2. Add code to `main.js` to:
    - Prompt for the multiple.
    - Use a loop to sum all multiples less than 100.
    - Output the result.
3. Save the file.
4. Open the terminal.
5. Run `npm i` to install the needed dependencies.
6. Run your program with the command: `node main.js`
7. (Optional) Use the `npm run styleCheck` command.
8. Test your program using `npm run testBed`.
9. Submit your assignment as described in the course instructions.

---

### Author Notes

- This assignment reinforces loop syntax and logic in both C++ and JavaScript.
- Practice using loops to solve problems and validate user input.
- Understanding loops is essential for all future programming assignments.
